import React from 'react';

const Header = () => {
    return (
        <div style={{padding:'20px 0',backgroundColor:'white',display:'flex',justifyContent:'center'}}>
            <img src="/pictures/logo_.png" alt="logo" style={{margin:'auto'}} />
        </div>
    );
};

export default Header;